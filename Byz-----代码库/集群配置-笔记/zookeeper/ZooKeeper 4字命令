
 命令都是四个字母,conf，cons，crst，dump，envi，ruok，stat，srvr，srst，wchc 等命令。

   mntr挺有用

～bin# telnet [host] 2181
 然后执行： stat   (可以看到一串输出，则说明服务端启动成功了)


另一种方法：
	bin# echo stat | nc biyuzhe 2181


conf: 输出服务器运行时s使用的基本配置信息，单机和集群模式输出不同

cons：输出当前z这台服务器上的所有连接的详细信息，包括ip..

crst: 功能型指令，重置所有的客户端连接信息

dump： 输出当前集群的所有会话信息，包括这些会话的id,及每个会话的临时节点信息

envi：输出zoo所在服务器运行时的环境信息，

ruok：输出当前zoo服务器是否正在运行

stat: 获取运行时基本状态信息he打印当前服务器的链接信息

srvr：与stat 唯一区别是 不将服务端的链接情况输出，只输出服务器的自身信息

srst：功能性指令，重置所有服务器的统计信息

wchcs: 输出当前服务器用于管理的wchc概要信息

wchc：输出当前服务器上管理的wchc的详细信息,以会话为单位

wchp： 输出当前服务器上管理的wchc的详细信息,以节点路径为单位

mntr: 输出比stat更为详细,包括请求处理，延时情况，服务器内存数据库的大小，集群服务器的同步情况，每一行输出都是（k,v）







